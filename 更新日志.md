# 更新日志

## v1.4.3 — 演示数据标注样式优化 (2026-09-10)

1. 全局「演示数据 · 非真实业务」标签重设计：由浅灰小字改为琥珀色胶囊标签（深棕文字 + 暖黄底），更醒目标识非真实业务数据；
2. hero 对话框：演示标签移出顶栏，作为整体悬浮于对话框正上方的居中位置，随对话框一起上下浮动，两侧新增黄色左右移动箭头；
3. hero 对话框内容区与顶栏内边距微调（`.term-body` 高 520px、`.term-bar` 内边距压缩、容器外边距调整），上下留白更均衡；
4. 交互展示（#interact）演示标签移至对话框顶栏水平居中，两侧同样带黄色左右移动箭头。

## v1.4.2 — 首页优化评审修复：性能 + 无障碍 + SEO (2026-09-10)

> 本版针对首页优化评审（P0/P1/P2）逐项落地修复，详见下文各分类。跳过的 P1-2（移动端 nav 重叠）、P1-3（终端表格窄屏截断）因移动端采用 zoom 等比缩放 1600px 画布，不存在重叠/截断问题，无需改动。

### 性能（P0-1）

1. ECharts 引入由「同步 `<script src=jsdelivr>`」改为异步动态加载：登录态走 58 内网 CDN 优先、jsdelivr 兜底的加载队列，彻底消除境外 CDN 挂起时的渲染阻塞；ECharts 就绪后自动补齐此前未能初始化的图表实例，确保图表不因延迟加载而漏渲染。

### 无障碍（P1-1 / P2-1 / P2-2 / P2-4）

1. `.reveal` 滚动入场动画补 JS 兜底：无 `IntersectionObserver` 时全部元素直接可见，页面加 `<noscript>` 兜底保证禁用脚本时主体正常显示；
2. 技能弹窗完善键盘可达性：打开时记录触发焦点并在关闭后返回，聚焦落到关闭按钮，Tab 循环圈定在弹窗内；
3. 数据源技能卡片支持键盘操作：增加 `role="button"` 与 `tabindex="0"`，支持 Enter / Space 触发弹窗；
4. reduced-motion 偏好覆盖 JS 动画：用户开启「减弱动态效果」时，hero 自动轮播关闭、终端逐行呈现跳过延迟。

### SEO / 细节（P2-3 / P2-5 / P2-6）

1. 补充 `meta description` 与 Open Graph（og:title / og:description / og:type）标签；
2. 低对比度小字加深：`--faint` `#9aa0aa` → `#7d8696`、`.ln .dim` `#98a1b3` → `#7d8696`，改善极小字号可读性；
3. 演示区（hero 终端 / 交互演示）新增「演示数据 · 非真实业务」浅色标注，明确可视化内容为演示而非真实业务数据。

## v1.4.1 — 全站交互修复 + 复制文案统一 (2026-09-08)

### 稳定性

1. 修复刷新到非首页路由（Skills / 常见问题 / 更新日志）时，悬浮目录与返回顶部按钮偶发偏移：`fit()` 原用 `querySelector` 选中 `display:none` 的首页 section，其 `getBoundingClientRect()` 全为 0，导致 left/right 计算异常；改为遍历取第一个可见 `.wrap`，并在 rAF 内重读当前 `zoom` 值，规避多次 fit 调用间闭包变量过时；
2. 修复首页数据源卡片弹窗「复制命令」点击无反应：原 `c.parentElement.querySelector('.cmd')` 选择器因 `.cmd` 是兄弟节点取不到而静默抛错；改为 `c.closest('.mcmd').querySelector('.cmd')`，并以防重复点击判断由脆弱的文案比对改为 `copied` 类名；
3. 修复从其它 tab 切回首页时 hero 对话框对话停止：`render()` 切走时 `stop()` 清掉了递归 `setTimeout` 播放链，切回却不重启；新增首次渲染标记，切回首页时自动重启 hero 演示；
4. 修复更新日志页返回顶部按钮进度环不转：`update()` 遗漏了 changelog 进度环 `ringChangelog` 的更新；
5. 交互演示（#interact）改为「进入视口自动播放、离开视口停止」，移除一次性 `unobserve`，切走再切回首页滚动到该区可重新触发播放。

### 性能

1. `update()` 原生每次滚动都重复 `querySelectorAll('.closing, footer')` 并重建浮层元素数组；两者不随生命周期变化，已缓存到闭包层，减少滚动高频回调的 DOM 查询开销。

### 文案统一

1. 数据源卡片弹窗复制按钮由「复制命令」统一为首页 / 安装器一致的「⧉ 复制」；
2. 「工作区沉淀」「数据沉淀」等文案统一为「工作区保存 / 数据保存」，保持全站「保存」口径；
3. 首页页脚版本号对齐为 v1.4.0。

## v1.4.0 — 悬浮目录 + 返回顶部 + Liquid Glass + 深色适配 (2026-09-08)

### 新增

1. 首页新增左侧悬浮目录（GlassMorphism）：8 个目录项（快速上手 / 工作流程 / 覆盖平台 / 能力矩阵 / 交互演示 / 信任边界 / 机制保障 / 出品团队），蓝色高亮 + 渐变纵向竖条标记当前区块，点击平滑滚动定位；
2. 各视图新增右侧圆形返回顶部按钮：滚动过半屏淡入出现，带进度环，悬停放大，点击平滑回到顶部；首页 / Skills / 更新日志三视图样式逐像素统一；
3. 悬浮目录与返回顶部支持深色区域自动切换（进入结语区 / 页脚变深色玻璃样式），宽屏（≥1520px 或 zoom 激活）才显示，pinch-zoom 缩至 0.5 以下自动隐藏。

### 稳定性

1. 悬浮元素水平定位改为动态测量 section 内容区留白并除以 `zoom` 补偿，在不同缩放倍率下保持与内容区对称居中；
2. 「出品团队」跳转用 `offsetTop`（不受 zoom 影响）让区块中心对齐视口中心，规避缩放下目录与区块错位。

## v1.3.1 — Skills 安装器对齐 + 交互演示图表动画修复 (2026-09-07)

### Skills 安装器 / 弹窗 UI 对齐

1. 独立版安装选项改为三列网格并整体居中（`justify-content:center`），三列按内容收拢，复选框/中文名/英文名逐列纵向对齐，消除单列右侧大空白；
2. 整合版功能要点二列网格同样改为整体居中，减少两侧空白；新增第 4 条功能「自动更新到最新版」，四格铺满 2×2；
3. 技能弹窗命令卡改为与首页一致的深蓝代码卡片：顶部 `bash` 标签 + 复制按钮，命令正文居左，复制按钮不再独立排在下方；
4. 复制命令按钮样式全站统一为带边框、悬停态、复制成功变绿「已复制 ✓」的反馈风格。

### 首页

1. hero 顶部按钮「一条链路 →」更名为「交互场景 →」，跳转目标改为交互展示区（#interact）；
2. hero 主按钮右侧新增「快速接入」深蓝实底按钮，点击跳转「快速开始」代码框（#start）；
3. 底部结语区引导文案不再换行（放宽宽度 + nowrap），「先看常见问题」按钮更名「常见问题」；
4. 首页代码块（.q-cmd）统一为 Skills 同款配色与字体（深蓝底、蓝调文字、--mono 字体）。

### 其它

1. 新增 favicon（复用顶部 logo 图案），收窄 SVG viewBox 使图标更饱满；
2. section 副标题可用宽度 640→820px，统一断行表现。

### 稳定性

1. 修复交互演示中「数据预测」趋势图入场不带动画的问题：ECharts 实例化时机由「消息隐藏时」改为「消息行 reveal 可见时」，入场动画可正常播放；非动画初始渲染路径补上图表初始化，避免漏渲染。

## v1.3.0 — 首页改版 + 「快速开始」引导 + Skills 安装器简化 (2026-09-07)

### 首页文案与结构优化

1. 「零门槛，装好就能直接开问」区块：副标题承接"不用理解技术差异"，突出 0 门槛、装完即开问；
2. 三步引导第 2 步更新为「说出你的需求 / 直接告诉它你的数据需求是什么，维度/周期/指标，分析重点」；
3. 「一个入口，六个数据源」→「一个入口，连接所有数据源」，强调覆盖所有平台而非数量；
4. 「复杂分析，一气呵成」区块前移至「一个入口，连接所有数据源」之前，并按移动后的顺序重新校正各区块灰/白斑马纹交替。

### 新增「快速开始」安装引导

1. 新增安装引导卡：左侧「一键安装」说明卡 + 右侧命令卡（推荐方式 / 「查看详情 →」跳转 Skills / 深色代码框 + 复制按钮）；
2. 分隔小标题「快速开始」，两侧为蓝色 `<<` `>>` 箭头并带向外扩张动画（SmartBoard 风格）；
3. 安装卡与上方 STEP 卡宽度对齐。

### Skills 安装器简化

1. 安装方式由三档简化为两档：整合版 / 独立版；
2. 独立版复选框默认全选安装，取消勾选即按需（命令自动拼接所选 id）；
3. 复选框网格 3 列 × 2 行；移除冗余「适配状态」文字行（适配状态由下方平台卡展示）。

### 稳定性

1. 安装卡内容区固定 `min-height`，切换选项卡时卡片高度不再跳动；
2. 修复联动 flex 布局导致复选框网格缩窄偏左的问题（网格 `width:100%` 铺满）；
3. 浏览器实测整站路由/弹窗/演示/安装器均正常，无 console 报错。

## v1.2.3 — 单文件重构为 分目录（css/js/font） + 全局 Bug 修复 (2026-09-07)

### 背景

原 `index.html` 为单个 ~170KB 的大文件（HTML+CSS+JS+字体引用全部内联），难以维护且存在多个隐患。本次将项目重构为「分工明确的目录结构」，并在拆解过程中顺带修复了原文件中长期存在的 6 项 bug 与 9 处冗余代码。

### 改动

1. **目录结构分拆**：
   - `index.html` 置于项目根目录，只保留页面结构与 CSP 安全头；
   - `css/styles.css`：全部样式（含 PingFang SC 字体引入、Impact 装饰字体 base64 内联）；
   - `js/app.js`：核心页面逻辑（路由 / 演示播放器 / 弹窗 / 安装器 / 缩放）；
   - `js/demo-data.js`：演示对话、图表配置、Skill 元数据等共享数据（Hero 与交互演示复用，避免重复维护）；
   - `font/PingfangSC-subset.woff2`：PingFang SC 子集字体，独立存放，CSS 通过 `../font/` 相对路径引用。
2. **Bug 修复**：
   - `document.write` 动态补载 ECharts → 改为创建 `<script>` 元素加载，规避同步阻塞；
   - 切走首页时 Hero 演示定时器持续空转 → 路由切换时显式 `stop()`；
   - 视口缩放用内联脚本导致的闪烁 → 统一收敛为单一 `fit()`，页面加载时一次性计算 `zoom`；
   - ECharts `init/setOption` 无异常保护 → 全部包裹 `try-catch`；
   - Hero 与交互演示重复的 `show()` → 用 `createDemoPlayer` 工厂统一；
   - `io` 命名与全局 Reveal 观察器冲突 → 重命名为 `scrollIO`。
3. **冗余清理**（9 处）：
   - 悬空 CSS 变量 `--ink`、`--panel-line` → 归位为 `--fg`、删除；
   - 未使用选择器 `.nav-links`、`.cat-head`、`.dv .code-en`、`.cl-week` → 删除；
   - 未触发的 `.revealing` 状态、未使用的 `@keyframes termIn / rowUp` → 删除；
   - 无点击交互的 `.tag-chip` 移除 `cursor:pointer` 与伪 hover。
4. **性能**：ECharts CDN 从 `<head>` 移到底部，不再阻塞首屏渲染。

### 验证

- 本地 HTTP 服务器实测：四个页面（首页 / Skills / 常见问题 / 更新日志）路由切换、Skill 弹窗、演示轮播均正常，全程无 JS 控制台报错；
- PingFang SC 字体经 `font/` 相对路径正常加载；
- 已 commit（c51b831）并 push 至 `hbg-preview` 远端 `main` 分支。